# AgentLens Architecture

AgentLens sits between AI agents and the tools they call.

It captures structured events during agent execution and streams them to a dashboard.

Core components

Instrumentation Layer  
Wraps agent actions and records events.

Event Stream  
Stores structured events.

Dashboard  
Displays agent activity timelines.
